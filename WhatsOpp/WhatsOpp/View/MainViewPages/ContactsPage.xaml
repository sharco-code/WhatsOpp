<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             BackgroundColor="AliceBlue"
             x:Class="WhatsOpp.View.MainViewPages.ContactsPage">

    <ContentPage.Content>
        <StackLayout>

            <Frame VerticalOptions="FillAndExpand" >
                <StackLayout Orientation="Horizontal">
                    <Entry Placeholder="Nombre de usuario" Text="{Binding AddContactSendDTO.Username}"/>
                    <Button Text="Añadir contacto" TextColor="Blue" BackgroundColor="AliceBlue"  Clicked="AddContact_Clicked"/>
                </StackLayout>
            </Frame>


            <Frame >



                <ListView ItemTapped="ContactList_ItemTapped" ItemsSource="{Binding ContactList}">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <TextCell Text="{Binding Username}" Detail="{Binding Name}"/>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>

                    
            </Frame>
           

        </StackLayout>
    </ContentPage.Content>
</ContentPage>